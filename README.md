# Koji Image Builder

This project provides [Image Builder](https://osbuild.org) integration with [Koji](https://koji.build/). It makes it possible to build images and other operating system artifacts via using the local [image-builder](https://github.com/osbuild/image-builder-cli) executable in Koji.
